apiVersion: v1
kind : Pod 
metadata:
 name: onfailure
spec:
 restartPolicy: onfailure
 containers: 
 - name: restratalways
   image: busybox
   command : ["sh", "-c"," echo busy busy && sleep 30 "]
 
