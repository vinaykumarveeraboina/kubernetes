apiVersion: v1
kind : Pod 
metadata:
 name: alwaysRestart
spec:
 restartPolicy: Always
 containers: 
 - name: restratalways
   image: busybox
   command : ["sh", "-c"," echo busy busy && sleep 30 "]
 
