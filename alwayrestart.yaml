apiVersion: v1
kind : Pod 
metadata:
 name: alwaysRestart
spec:
 restratPolicy: Always 
 container: 
 - name: restratalways
   image: busybox
   command : ["sh", "-c"," echo busy busy && sleep 30 "]
 
