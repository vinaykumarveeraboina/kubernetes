apiVersion: v1
kind : Pod 
metadata:
 name: alwaysRestart
spec:
 restartPolicy: Always 
 container: 
 - name: restratalways
   image: busybox
   command : ["sh", "-c"," echo busy busy && sleep 30 "]
 
