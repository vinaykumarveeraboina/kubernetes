apiVersion: v1
kind: pod
metadata:
  name: livenessfailure
spec:
 containers:
 - image: nginx
   name : nginx
   livenessProbe:
    httpGet:
     path: /vinay
     port: 80
    initialDelaySeconds: 10
    periodSeconds: 10 
   
